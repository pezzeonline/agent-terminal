// Project-level task runner. Real subcommands land in follow-up commits;
// this initial stub just proves the workspace + `cargo xtask` alias resolve.

fn main() {
    println!("xtask: no subcommands wired yet");
}
